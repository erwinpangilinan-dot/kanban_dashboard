{
  "name": "mission-control",
  "version": "1.0.0",
  "private": true,
  "description": "Mission Control Kanban Dashboard",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "NODE_ENV=production npm run start --prefix backend",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:up:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/start-windows.ps1",
    "docker:down:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/stop-windows.ps1",
    "docker:logs:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/logs-windows.ps1",
    "setup:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/setup-windows.ps1",
    "mcp:install": "npm install --prefix mcp",
    "mcp:verify": "node mcp/scripts/verify-mcp.js",
    "test": "npm test --prefix backend",
    "ci": "npm test --prefix backend && npm run verify:auth --prefix backend && npm run verify:notify --prefix backend && npm run verify:ops --prefix backend && npm run verify:email-assistant --prefix backend && npm run migrate --prefix backend && node scripts/ci-smoke.js && npm run build --prefix frontend && npm run mcp:verify",
    "db:up": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d postgres",
    "db:down": "docker compose down",
    "setup": "cp -n .env.example .env 2>/dev/null || true && npm run db:up && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
